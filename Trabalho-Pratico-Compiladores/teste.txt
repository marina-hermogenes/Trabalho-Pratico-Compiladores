/* ---------- CASOS VÁLIDOS ---------- */
int x = 10;
bool flag = true, toggled = false;
int y;
y = x + 5 * (2 + -3);
a = -42;

/* print com expressões e literais */
print("Resultado: ", x, -1);
read(y);

{
    int a = 1, b = 2;
    print(a, ", ", b);
    {
        int inner = -5;
    }
}

if (x > 0)
    if (y > 0)
        print("both positive");
    else
        print("y non-positive");

while (x < 3) {
    x = x + 1;
    print(x);
}

/* ---------- CASOS QUE DEVEM GERAR ERRO (sincronizam com ';') ---------- */
z = ;
w = 5 + * 3;
p = (4 + 2;
q = a && || b;
r = !;
print(, x);
print("ok", , );
read();
while x < 5;
if (a > ) ;
if (flag) print("ok");
else
int ;
s = + - * / % ;
t = ( ) ;
u = (3 + );
v = a * (b + );
{
    int e = 5;
    e = e + ;
    print(e

/* teste de múltiplos erros seguidos para ver se parser recupera */
x = 1 + *;
y = ;
print("fim", );

/* fim do arquivo com caso válido para confirmar recuperação */
int ok_final = 100;
print("done", ok_final);